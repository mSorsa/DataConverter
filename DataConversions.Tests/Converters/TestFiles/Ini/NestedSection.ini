; Note: Standard INI format doesn't support nested sections,
; but this is how some might represent them for visualization.

[Network]
[Network.HTTP]
enable=true
timeout=30

[Network.FTP]
enable=false
port=21
